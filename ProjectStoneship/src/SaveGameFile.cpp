/*
 * SaveGameFile.cpp
 *
 *  Created on: Nov 11, 2015
 *      Author: zalasus
 */

#include "SaveGameFile.h"

namespace Stoneship
{

	void SaveGameFile::load()
	{
		/*mInputStream.open(mFilename.c_str(), std::ios::in | std::ios::binary);

		if(!mInputStream.good())
		{
			STONESHIP_EXCEPT(StoneshipException::IO_ERROR, "File not found");
		}


		MGFDataReader ds(&mInputStream, this);

		uint64_t magic;
		ds.readULong(magic);
		if(magic != 0x464753334750524E) // "NRPG3SGF"
		{
			STONESHIP_EXCEPT(StoneshipException::DATA_FORMAT, "Not a Save Game File: Invalid Magic ID");
		}*/



	}

}

